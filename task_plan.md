# 腾讯云自动部署任务计划

## 目标

为 Phrase Bank 配置 GitHub Actions 自动部署到 `43.153.204.17`，通过 HTTP 80 访问，并确保测试失败不影响现网版本。

## 阶段

- [x] 阶段 1：编写并自检实施计划
- [x] 阶段 2：测试驱动创建 Docker、Compose 与工作流配置
- [x] 阶段 3：配置服务器 SSH 密钥与 Docker 环境
- [x] 阶段 4：配置 GitHub Actions Secrets
- [ ] 阶段 5：触发部署并验证服务器本地访问（进行中）
- [ ] 阶段 6：验证公网访问并完成安全收尾

## 关键约束

- 不在仓库、日志或聊天中保存服务器密码和 SSH 私钥。
- 不关闭现有 SSH 登录方式。
- 仅开放应用所需的 TCP 80。
- 客户端 IndexedDB 数据不受服务器更新影响。
- 所有代码变更先测试再实施。

## 已遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| GitHub 插件安装失败 | 1 | 改用 GitHub CLI 与专用 SSH 密钥 |
| GitHub CLI 网页授权网络失败 | 1 | 通过 SSH 推送并由用户网页创建仓库 |
| root SSH 登录被拒绝 | 1 | 确认正确用户为 ubuntu |
| Windows Set-Clipboard 被占用 | 1 | 改用系统 `clip.exe` 写入，不输出私钥 |
